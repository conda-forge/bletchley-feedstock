{% set name = "bletchley" %}
{% set version = "0.2.0" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: https://gitlab.com/manny_cyber_wizard/{{ name }}/-/archive/{{ version }}/{{ name }}-{{ version }}.tar.bz2
  sha256: 26a3fdc6626fefb2c4afee2305d71d98122d21cc02ca2a5ba7f341b4358cd4fc

build:
  number: 0
  noarch: python
  script: "{{ PYTHON }} -m pip install . --no-deps --ignore-installed -vv "

requirements:
  host:
    - pip
    - python
  run:
    - numpy >=1.16.0
    - python

test:
  imports:
    - bletchley
    - bletchley.ciphers
    - bletchley.cryptanalysis

about:
  home: https://gitlab.com/manny_cyber_wizard/bletchley
  license: GNU-3.0
  license_family: GPL
  license_file: LICENSE
  summary: A collection of historical ciphers and cryptanalysis tools
  doc_url: https://gitlab.com/manny_cyber_wizard/bletchley/wikis/home
  dev_url: https://gitlab.com/manny_cyber_wizard/bletchley

extra:
  recipe-maintainers:
    - marcelotrevisani
    - mannycyber
